// Code generated by codegen/generate_cli.py; DO NOT EDIT.

package calling

import (
	"fmt"
	"strconv"
	"strings"

	cmd "github.com/Cloverhound/webex-cli/cmd"
	"github.com/Cloverhound/webex-cli/internal/client"
	"github.com/Cloverhound/webex-cli/internal/config"
	"github.com/Cloverhound/webex-cli/internal/output"
	"github.com/spf13/cobra"
)

// Ensure imports are used.
var _ = fmt.Sprintf
var _ = config.Token
var _ = output.Print
var _ = strconv.Itoa
var _ = strings.Join

var modeManagementCmd = &cobra.Command{
	Use:   "mode-management",
	Short: "ModeManagement commands",
}

func init() {
	cmd.CallingCmd.AddCommand(modeManagementCmd)

	{ // get-features
		cmd := &cobra.Command{
			Use:   "get-features",
			Short: "Get Mode Management Features",
			Long:  "Retrieves a list of all mode management features (Auto Attendants, Call Queues, and Hunt Groups) for which the authenticated user has been designated as a mode manager. This API returns basic information about each feature including its ID, name, and type.\n\nMode Management allows designated managers to switch features between different operational configurations based on time schedules or manual triggers. This is useful for managing business hours, holidays, and emergency scenarios.\n\nThis API requires a user auth token with the `spark:telephony_config_read` scope. The authenticated user must be configured as a mode manager for at least one feature to receive results.",
			RunE: func(cmd *cobra.Command, args []string) error {
				req := client.NewRequest(config.CallingBaseURL, "GET", "/telephony/config/people/me/settings/modeManagement/features")
				if config.Paginate() {
					resp, statusCode, err := req.DoPaginated(true)
					if err != nil {
						return err
					}
					return output.Print(resp, statusCode)
				}
				resp, statusCode, err := req.Do()
				if err != nil {
					return err
				}
				return output.Print(resp, statusCode)
			},
		}
		modeManagementCmd.AddCommand(cmd)
	}

	{ // get-common
		var featureIds string
		cmd := &cobra.Command{
			Use:   "get-common",
			Short: "Get Common Modes",
			Long:  "Retrieves a list of common operating mode names that are shared across multiple specified features. This API accepts a list of feature IDs and returns only the mode names that exist in all of the specified features, allowing managers to switch multiple features to the same mode simultaneously.\n\nCommon modes are useful when you need to coordinate operational changes across multiple features. For example, switching an entire office to \"Holiday\" mode across all Auto Attendants and Call Queues at once.\n\nThis API requires a user auth token with the `spark:telephony_config_read` scope. The authenticated user must be a mode manager for the specified features.",
			RunE: func(cmd *cobra.Command, args []string) error {
				req := client.NewRequest(config.CallingBaseURL, "GET", "/telephony/config/people/me/settings/modeManagement/features/commonModes")
				req.QueryParam("featureIds", featureIds)
				if config.Paginate() {
					resp, statusCode, err := req.DoPaginated(true)
					if err != nil {
						return err
					}
					return output.Print(resp, statusCode)
				}
				resp, statusCode, err := req.Do()
				if err != nil {
					return err
				}
				return output.Print(resp, statusCode)
			},
		}
		cmd.Flags().StringVar(&featureIds, "feature-ids", "", "List of feature IDs (comma-separated) for auto attendants, call queues, or hunt groups")
		modeManagementCmd.AddCommand(cmd)
	}

	{ // switch-multiple-features
		var featureIds []string
		var operatingModeName string
		var bodyRaw string
		var bodyFile string
		cmd := &cobra.Command{
			Use:   "switch-multiple-features",
			Short: "Switch Mode for Multiple Features",
			Long:  "Switches the operating mode for multiple features simultaneously by specifying a common mode name. This API accepts a list of feature IDs and sets all of them to the specified operating mode, provided that mode exists for all features.\n\nThis bulk operation is particularly useful for coordinating operational changes across an organization, such as activating holiday modes, emergency procedures, or after-hours configurations across multiple Auto Attendants, Call Queues, and Hunt Groups at once.\n\nThis API requires a user auth token with the `spark:telephony_config_write` scope. The authenticated user must be a mode manager for all specified features.",
			RunE: func(cmd *cobra.Command, args []string) error {
				req := client.NewRequest(config.CallingBaseURL, "POST", "/telephony/config/people/me/settings/modeManagement/features/actions/switchMode/invoke")
				if bodyFile != "" {
					if err := req.SetBodyFile(bodyFile); err != nil {
						return err
					}
				} else if bodyRaw != "" {
					req.SetBodyRaw(bodyRaw)
				} else {
					req.BodyStringSlice("featureIds", featureIds)
					req.BodyString("operatingModeName", operatingModeName)
				}
				resp, statusCode, err := req.Do()
				if err != nil {
					return err
				}
				return output.Print(resp, statusCode)
			},
		}
		cmd.Flags().StringSliceVar(&featureIds, "feature-ids", nil, "")
		cmd.Flags().StringVar(&operatingModeName, "operating-mode-name", "", "")
		cmd.Flags().StringVar(&bodyRaw, "body", "", "Raw JSON body")
		cmd.Flags().StringVar(&bodyFile, "body-file", "", "Path to JSON body file")
		modeManagementCmd.AddCommand(cmd)
	}

	{ // get-feature
		var featureId string
		cmd := &cobra.Command{
			Use:   "get-feature",
			Short: "Get Mode Management Feature",
			Long:  "Retrieves detailed information about a specific mode management feature including its current operating mode and exception status. This API provides the feature's ID, name, type, current operating mode ID, and whether it is currently in an exception mode.\n\nException mode indicates that the feature has been manually switched to a different mode than what its schedule dictates. This information is critical for mode managers to understand the current state of their features.\n\nThis API requires a user auth token with the `spark:telephony_config_read` scope. The authenticated user must be a mode manager for the specified feature.",
			RunE: func(cmd *cobra.Command, args []string) error {
				req := client.NewRequest(config.CallingBaseURL, "GET", "/telephony/config/people/me/settings/modeManagement/features/{featureId}")
				req.PathParam("featureId", featureId)
				if config.Paginate() {
					resp, statusCode, err := req.DoPaginated(true)
					if err != nil {
						return err
					}
					return output.Print(resp, statusCode)
				}
				resp, statusCode, err := req.Do()
				if err != nil {
					return err
				}
				return output.Print(resp, statusCode)
			},
		}
		cmd.Flags().StringVar(&featureId, "feature-id", "", "Unique identifier for the feature.")
		cmd.MarkFlagRequired("feature-id")
		modeManagementCmd.AddCommand(cmd)
	}

	{ // get-normal-operation
		var featureId string
		cmd := &cobra.Command{
			Use:   "get-normal-operation",
			Short: "Get Normal Operation Mode",
			Long:  "Retrieves the current normal operating mode that the feature is scheduled to be in based on its time schedules. This represents the mode the feature would be in if no manual exceptions or overrides were active.\n\nThe normal operation mode is determined by the feature's configured schedules and may differ from the actual current operating mode if a manual exception has been applied. This API helps managers understand what the scheduled behavior is versus the actual current state.\n\nThis API requires a user auth token with the `spark:telephony_config_read` scope. The authenticated user must be a mode manager for the specified feature.",
			RunE: func(cmd *cobra.Command, args []string) error {
				req := client.NewRequest(config.CallingBaseURL, "GET", "/telephony/config/people/me/settings/modeManagement/features/{featureId}/normalOperationMode")
				req.PathParam("featureId", featureId)
				if config.Paginate() {
					resp, statusCode, err := req.DoPaginated(true)
					if err != nil {
						return err
					}
					return output.Print(resp, statusCode)
				}
				resp, statusCode, err := req.Do()
				if err != nil {
					return err
				}
				return output.Print(resp, statusCode)
			},
		}
		cmd.Flags().StringVar(&featureId, "feature-id", "", "Unique identifier for the feature.")
		cmd.MarkFlagRequired("feature-id")
		modeManagementCmd.AddCommand(cmd)
	}

	{ // get-operating
		var featureId string
		var modeId string
		cmd := &cobra.Command{
			Use:   "get-operating",
			Short: "Get Operating Mode",
			Long:  "Retrieves detailed information about a specific operating mode for a feature, including the mode's ID and name. This API allows managers to get the details of any operating mode configured for a feature.\n\nOperating modes define different configurations for how a feature behaves (e.g., business hours routing vs. after-hours routing). Each mode has a unique ID and a descriptive name that helps managers identify its purpose.\n\nThis API requires a user auth token with the `spark:telephony_config_read` scope. The authenticated user must be a mode manager for the specified feature.",
			RunE: func(cmd *cobra.Command, args []string) error {
				req := client.NewRequest(config.CallingBaseURL, "GET", "/telephony/config/people/me/settings/modeManagement/features/{featureId}/modes/{modeId}")
				req.PathParam("featureId", featureId)
				req.PathParam("modeId", modeId)
				if config.Paginate() {
					resp, statusCode, err := req.DoPaginated(true)
					if err != nil {
						return err
					}
					return output.Print(resp, statusCode)
				}
				resp, statusCode, err := req.Do()
				if err != nil {
					return err
				}
				return output.Print(resp, statusCode)
			},
		}
		cmd.Flags().StringVar(&featureId, "feature-id", "", "Unique identifier for the feature.")
		cmd.MarkFlagRequired("feature-id")
		cmd.Flags().StringVar(&modeId, "mode-id", "", "Unique identifier for the operating mode.")
		cmd.MarkFlagRequired("mode-id")
		modeManagementCmd.AddCommand(cmd)
	}

	{ // switch-normal-operation
		var featureId string
		cmd := &cobra.Command{
			Use:   "switch-normal-operation",
			Short: "Switch to Normal Operation",
			Long:  "Switches the feature back to its normal scheduled operation mode, removing any manual exceptions or overrides that may be active. This returns the feature to operating according to its configured time schedules.\n\nThis operation is useful when a temporary manual mode change (exception) is no longer needed and you want to restore automatic schedule-based operation. It effectively cancels any active manual mode switches.\n\nThis API requires a user auth token with the `spark:telephony_config_write` scope. The authenticated user must be a mode manager for the specified feature.",
			RunE: func(cmd *cobra.Command, args []string) error {
				req := client.NewRequest(config.CallingBaseURL, "POST", "/telephony/config/people/me/settings/modeManagement/features/{featureId}/actions/switchToNormalOperation/invoke")
				req.PathParam("featureId", featureId)
				resp, statusCode, err := req.Do()
				if err != nil {
					return err
				}
				return output.Print(resp, statusCode)
			},
		}
		cmd.Flags().StringVar(&featureId, "feature-id", "", "Unique identifier for the feature.")
		cmd.MarkFlagRequired("feature-id")
		modeManagementCmd.AddCommand(cmd)
	}

	{ // switch-single-feature
		var featureId string
		var operatingModeId string
		var isManualSwitchbackEnabled bool
		var bodyRaw string
		var bodyFile string
		cmd := &cobra.Command{
			Use:   "switch-single-feature",
			Short: "Switch Mode for Single Feature",
			Long:  "Switches the operating mode for a single feature to a specified mode, either temporarily or with manual switchback. This API creates an exception to the feature's normal scheduled operation, allowing managers to manually control the feature's behavior.\n\nYou can configure whether the mode switch is temporary (automatically reverts based on schedule) or requires manual switchback. This is useful for handling unexpected situations like emergency closures, special events, or unscheduled breaks.\n\nThis API requires a user auth token with the `spark:telephony_config_write` scope. The authenticated user must be a mode manager for the specified feature.",
			RunE: func(cmd *cobra.Command, args []string) error {
				req := client.NewRequest(config.CallingBaseURL, "POST", "/telephony/config/people/me/settings/modeManagement/features/{featureId}/actions/switchMode/invoke")
				req.PathParam("featureId", featureId)
				if bodyFile != "" {
					if err := req.SetBodyFile(bodyFile); err != nil {
						return err
					}
				} else if bodyRaw != "" {
					req.SetBodyRaw(bodyRaw)
				} else {
					req.BodyString("operatingModeId", operatingModeId)
					req.BodyBool("isManualSwitchbackEnabled", isManualSwitchbackEnabled, cmd.Flags().Changed("is-manual-switchback-enabled"))
				}
				resp, statusCode, err := req.Do()
				if err != nil {
					return err
				}
				return output.Print(resp, statusCode)
			},
		}
		cmd.Flags().StringVar(&featureId, "feature-id", "", "Unique identifier for the feature.")
		cmd.MarkFlagRequired("feature-id")
		cmd.Flags().StringVar(&operatingModeId, "operating-mode-id", "", "")
		cmd.Flags().BoolVar(&isManualSwitchbackEnabled, "is-manual-switchback-enabled", false, "")
		cmd.Flags().StringVar(&bodyRaw, "body", "", "Raw JSON body")
		cmd.Flags().StringVar(&bodyFile, "body-file", "", "Path to JSON body file")
		modeManagementCmd.AddCommand(cmd)
	}

	{ // extend-current-operating-duration
		var featureId string
		var operatingModeId string
		var extensionTime int64
		var bodyRaw string
		var bodyFile string
		cmd := &cobra.Command{
			Use:   "extend-current-operating-duration",
			Short: "Extend Current Operating Mode Duration",
			Long:  "Extends the duration of the current operating mode by adding additional time before it expires or reverts to scheduled operation. This API allows managers to prolong a temporary mode change without having to switch modes again.\n\nExtension time can be specified in 30-minute increments up to 720 minutes (12 hours). If no extension time is provided, the mode is extended with a manual switchback exception, meaning it will remain active until manually changed.\n\nThis API requires a user auth token with the `spark:telephony_config_write` scope. The authenticated user must be a mode manager for the specified feature.",
			RunE: func(cmd *cobra.Command, args []string) error {
				req := client.NewRequest(config.CallingBaseURL, "POST", "/telephony/config/people/me/settings/modeManagement/features/{featureId}/actions/extendMode/invoke")
				req.PathParam("featureId", featureId)
				if bodyFile != "" {
					if err := req.SetBodyFile(bodyFile); err != nil {
						return err
					}
				} else if bodyRaw != "" {
					req.SetBodyRaw(bodyRaw)
				} else {
					req.BodyString("operatingModeId", operatingModeId)
					req.BodyInt("extensionTime", extensionTime, cmd.Flags().Changed("extension-time"))
				}
				resp, statusCode, err := req.Do()
				if err != nil {
					return err
				}
				return output.Print(resp, statusCode)
			},
		}
		cmd.Flags().StringVar(&featureId, "feature-id", "", "Unique identifier for the feature.")
		cmd.MarkFlagRequired("feature-id")
		cmd.Flags().StringVar(&operatingModeId, "operating-mode-id", "", "")
		cmd.Flags().Int64Var(&extensionTime, "extension-time", 0, "")
		cmd.Flags().StringVar(&bodyRaw, "body", "", "Raw JSON body")
		cmd.Flags().StringVar(&bodyFile, "body-file", "", "Path to JSON body file")
		modeManagementCmd.AddCommand(cmd)
	}

}
